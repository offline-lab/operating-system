# Disco

Disco is the service discovery and name resolution system for MoreOS. It lets nodes find each other by name without DNS servers, manual configuration, or internet.

See the [Discovery](discovery/) section for the full documentation: protocol spec, CLI reference, and configuration.
